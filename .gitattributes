# Vendored third-party data must survive every platform's line-ending
# configuration byte-for-byte: integrity gates hash the raw working-tree
# files. Disable ALL git text conversion for these pinned sources.
datasets/upstream/** -text
packages/ranking-core/src/legacy/** -text
packages/exercise-catalog/data/catalog.v1.json -text
packages/exercise-catalog/data/ranking-coverage-exceptions.json -text
